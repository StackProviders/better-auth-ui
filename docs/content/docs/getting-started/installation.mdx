---
title: Installation
icon: Download
---

import { Step, Steps } from "fumadocs-ui/components/steps";

Follow the steps below to set up `better-auth-ui` in your project. We recommend the **Manual Installation** via our Shadcn Registry for maximum control and performance.

<Steps>
<Step>
## Recommended: Manual Installation (Registry)

This is the **Pro-Level** approach. It installs the components directly into your project, giving you full control over the code and ensuring zero unnecessary dependencies.

### 1. Install All Auth Components

Includes sign-in, sign-up, forgot password, and internal hooks/lib.

<ShadcnInstall name="auth" />

### 2. Install Organization Management (Optional)

Required if you want to use team management and invitation flows.

<ShadcnInstall name="org" />

### 3. Install Account Settings (Optional)

Includes user profile, security settings, and account management cards.

<ShadcnInstall name="account" />

> [!TIP]
> Manual installation allows you to easily customize the UI to match your brand perfectly, as the code lives directly in your `components/`, `hooks/`, and `lib/` folders.

</Step>

<Step>
## Alternative: Quick Start (npm Package)

If you prefer a managed approach or want to get started instantly without managing the source code, you can install the library as a package.

```package-install
better-auth-ui@latest
```

### TailwindCSS Configuration

For [TailwindCSS](https://tailwindcss.com) `v4`, add the following `@import` to your global css:

```css title="styles/globals.css"
@import "better-auth-ui/css";
```

For [TailwindCSS](https://tailwindcss.com) `v3` _(Deprecated)_, add the following `content` to your Tailwind config:

```ts title="tailwind.config.ts"
content: ["./node_modules/better-auth-ui/dist/**/*.{js,ts,jsx,tsx,mdx}"];
```

</Step>

<Step>
### Integrations

Once you've installed the components, follow one of the guides below to integrate with your framework.

<Cards>
  <Card href="/integrations/next-js" title="Next.js" />
  <Card href="/integrations/tanstack-start" title="TanStack Start" />
  <Card href="/integrations/react" title="React" />
</Cards>
</Step>
</Steps>

## Next Steps

Now that you're set up, you might want to explore:

- [Customizing Auth Paths](/advanced/custom-auth-paths)
- [Adding Custom Fields](/advanced/additional-fields)
- [Global Localization](/advanced/localization)
- [API Reference](/api-reference/auth-ui-provider)
